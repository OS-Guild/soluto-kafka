{
  "scripts": {
    "teardown": "docker-compose down --remove-orphans",
    "setup": "docker-compose pull && docker-compose up -d",
    "test": "npm run teardown && npm run setup && npm run jest && npm run teardown",
    "format": "prettier --write '**/*.{ts,js,json}'",
    "jest": "jest --runInBand --config jest.config.local.json --testResultsProcessor jest-teamcity-reporter"
  },
  "dependencies": {
    "@types/jest": "^24.0.12",
    "@types/node": "^11.13.8",
    "@types/node-fetch": "^2.3.3",
    "@types/uuid": "^3.4.4",
    "delay": "^4.2.0",
    "jest": "^24.7.1",
    "jest-cli": "^24.7.1",
    "jest-teamcity-reporter": "^0.9.0",
    "node-fetch": "^2.5.0",
    "prettier": "^1.17.0",
    "tslib": "^1.9.3",
    "typescript": "^3.4.5",
    "uuid": "^3.3.2"
  }
}
